apiVersion: v1
kind: Service
metadata:
  name: group-service
  namespace: pim
spec:
  selector:
    app: group-service
  ports:
    - name: grpc
      port: 9014
      targetPort: 9014
    - name: http
      port: 9004
      targetPort: 9004
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: group-service
  namespace: pim
spec:
  replicas: 3
  selector:
    matchLabels:
      app: group-service
  template:
    metadata:
      labels:
        app: group-service
    spec:
      initContainers:
        - name: wait-deps
          image: busybox:1.36
          command:
            - sh
            - -c
            - until nc -z postgres 5432 && nc -z redis 6379 && nc -z kafka 9092 && nc -z etcd 2379; do sleep 2; done
      containers:
        - name: group-service
          image: pim/group-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: grpc
              containerPort: 9014
            - name: http
              containerPort: 9004
          envFrom:
            - configMapRef:
                name: pim-runtime
            - secretRef:
                name: pim-secrets
          env:
            - name: POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
            - name: SERVICE_INSTANCE_ID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          readinessProbe:
            httpGet:
              path: /health
              port: 9004
          livenessProbe:
            httpGet:
              path: /health
              port: 9004
            initialDelaySeconds: 30
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              memory: 512Mi
