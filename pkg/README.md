## pkg 目录说明

`pkg` 用于存放**可被多个服务复用的通用代码**，与某个具体业务服务无强耦合。例如：

- `common/`：公共常量、错误码、通用响应结构
- `middleware/`：Gin / gRPC 通用中间件（如链路追踪、日志、恢复）
- `config/`：通用配置加载逻辑（基于 viper 等）
- `protobuf/`：编译后的 gRPC 代码（如需集中管理）

当前阶段仅创建此说明文件，后续在你开始写具体代码时，再按需要逐步补充子目录和实现。

